name: anasoil_shared
description: Shared AnaSoil domain contracts and Flutter utilities.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
